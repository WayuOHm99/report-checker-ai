# บทเรียนจากสิ่งที่เคยพัง

ไฟล์นี้เก็บ **ปัญหาที่เกิดขึ้นจริงในโปรเจกต์นี้** ไม่ใช่ทฤษฎีทั่วไป ทุกบทอ้างอิง commit จริงที่ตรวจสอบย้อนหลังได้

**เขียนเพิ่มเมื่อไหร่:** หลังแก้ปัญหาใหญ่ทุกครั้ง โดยเฉพาะปัญหาที่ใช้เวลาหาสาเหตุนาน
**อ่านเมื่อไหร่:** ก่อนแตะส่วนที่เกี่ยวข้อง และก่อน deploy

---

## 1. ค่า config ที่แก้จาก dashboard หายไปเงียบ ๆ

**commit:** `fc59089` — 2026-08-03

**เกิดอะไรขึ้น:** มีการแก้ค่า `ALLOWED_ORIGIN` (รายชื่อเว็บที่ได้รับอนุญาตให้เรียก API) ผ่านหน้าเว็บ Cloudflare dashboard ให้เหลือแค่ `rubriclensai.com` ซึ่งเป็นโดเมนที่ยัง**ไม่ได้ต่อ DNS** ผลคือเว็บจริงที่คนใช้อยู่ (`rubriclensai.pages.dev`) ถูกเบราว์เซอร์บล็อกทันที ปุ่มเริ่มตรวจขึ้นข้อความดิบ ๆ ว่า `Failed to fetch`

**ทำไมถึงหายาก:** ค่าที่แก้จาก dashboard **ไม่ถูกบันทึกลง git** เปิดโค้ดดูก็เห็นค่าที่ถูกต้อง แต่ของจริงบนเซิร์ฟเวอร์เป็นอีกค่าหนึ่ง และข้อความ `Failed to fetch` ของเบราว์เซอร์ไม่ได้บอกว่าเป็นเรื่อง CORS เลย

**กฎที่ได้:**
- `wrangler.jsonc` คือแหล่งความจริงเดียวของ config ทั้งหมด **ห้ามแก้ค่า production ผ่าน dashboard เด็ดขาด**
- เวลาจะเปลี่ยนโดเมน ให้ใส่โดเมนใหม่**เพิ่ม** ไม่ใช่แทนที่ของเดิม จนกว่าโดเมนใหม่จะใช้งานได้จริง
- เจอ `Failed to fetch` ให้สงสัย CORS เป็นอันดับแรก

---

## 2. Gemini 3 คิดเพลินจนตอบไม่ทัน

**commit:** `1cfc762` — 2026-08-02

**เกิดอะไรขึ้น:** หลังย้ายมาใช้ Gemini 3 การตรวจเอกสารยาวเริ่มค้างจนหมดเวลา (timeout) สาเหตุคือ Gemini 3 ใช้ "โควตาคำตอบ" (`maxOutputTokens`) ร่วมกันระหว่าง**ความคิดภายในของโมเดล**กับ**คำตอบ JSON ที่ส่งกลับมาจริง** พอโมเดลคิดเยอะ โควตาก็หมดก่อนที่จะเขียน JSON เสร็จ

**แก้ยังไง:** ตั้ง `thinkingConfig: { thinkingLevel: 'low' }` เพราะการให้คะแนนตาม rubric เป็นงานทำตามคำสั่งที่มีกรอบชัดอยู่แล้ว ไม่ต้องคิดลึก แล้วขยายโควตาจาก `500 + (หัวข้อ × 250)` เป็น `1,000 + (หัวข้อ × 500)` เผื่อ JSON ภาษาไทยที่กินที่มากกว่าภาษาอังกฤษ

**กฎที่ได้:**
- เปลี่ยนรุ่นโมเดลทุกครั้ง ต้องทดสอบด้วย**เอกสารยาวที่สุดที่ระบบรับได้** ไม่ใช่เอกสารสั้น ๆ
- ทุกครั้งที่เรียกโมเดล ต้องมีเพดานเวลา อย่าปล่อยให้รอไม่จำกัด

---

## 3. จับ error รวมกันทีเดียว ทำให้แยกไม่ออกว่าพังเพราะอะไร

**commit:** `8362418` — 2026-08-01

**เกิดอะไรขึ้น:** โค้ดเดิมครอบทั้ง "การเรียก Gemini" และ "การแปลงคำตอบเป็น JSON" ไว้ใน `try` ก้อนเดียว ผลคือแยกไม่ออกว่าที่พังเป็นเพราะ **ต่อ Gemini ไม่ได้** หรือ **Gemini ตอบมาผิดรูป** ระบบเลยไปลองเรียกใหม่พร้อมข้อความ "ขอ JSON เท่านั้น" ทั้งที่ปัญหาจริงคือต่อไม่ติด = เสียเวลาและเสียเงินเรียกซ้ำฟรี ๆ

**แก้ยังไง:** แยกเป็นสองขั้น — ถ้าเรียกไม่ติดคืน `502` พร้อมข้อความว่าเชื่อมต่อไม่ได้ทันที ส่วนการแปลง JSON แยกออกมาเป็นฟังก์ชัน `safelyParseJson` ที่คืน `undefined` แทนที่จะโยน error

**กฎที่ได้:** `try/catch` หนึ่งก้อน ควรครอบ **ความล้มเหลวแบบเดียว** ถ้าในนั้นมีสองอย่างที่พังได้คนละสาเหตุ ให้แยกก้อน

---

## 4. นับ token ผิดเพราะลืมนับคำสั่งระบบ

**commit:** `682cec2` — 2026-08-01

**เกิดอะไรขึ้น:** โค้ดกันงบประมาณเรียก `countTokens` โดยส่ง `systemInstruction` (คำสั่งประจำที่บอกโมเดลว่าต้องทำตัวยังไง) ไปทาง `config` ซึ่งฝั่ง API **ไม่ได้เอามานับ** ผลคือระบบคิดว่าใช้ token น้อยกว่าความจริงทุกครั้ง — ด่านกัน 120,000 token จึงปล่อยเอกสารที่ใหญ่เกินจริงผ่านไปได้

**แก้ยังไง:** ต่อ `systemInstruction` เข้ากับ prompt เป็นข้อความเดียวก่อนนับ

**กฎที่ได้:** ตัวเลขที่ใช้เป็น "ด่านกัน" ต้องพิสูจน์ว่านับครบจริง อย่าเชื่อว่า API นับให้เหมือนที่เราคิด — ด่านที่นับผิดอันตรายกว่าไม่มีด่าน เพราะทำให้สบายใจผิด ๆ

---

## 5. เว็บ production รันด้วยข้อมูลตัวอย่างโดยไม่มีใครรู้

**commit:** `93f3c10` — 2026-08-01

**เกิดอะไรขึ้น:** โค้ดเดิมเช็คว่า `VITE_USE_MOCK_ANALYSIS !== 'false'` ถึงจะใช้ AI จริง แปลว่าถ้าตอน build **ลืมตั้งค่านี้** (ซึ่งเป็นค่าว่าง ไม่ใช่ `'false'`) เว็บ production จะใช้ **ข้อมูลตัวอย่างปลอม** แทน AI จริง โดยหน้าเว็บดูปกติทุกอย่าง

**แก้ยังไง:** เขียนฟังก์ชัน `usesMockAnalysis()` และ `getApiBaseUrl()` ที่มีค่าเริ่มต้นชัดเจน — ตอน dev ใช้ mock, ตอน production ใช้ของจริง — แล้วเอา comment ครอบค่าใน `.env.example` เพื่อให้ค่าเริ่มต้นทำงาน

**กฎที่ได้:**
- ค่าเริ่มต้นเมื่อ "ลืมตั้งค่า" ต้องเป็นค่าที่**ปลอดภัยและถูกต้อง** ไม่ใช่ค่าที่ทำให้ระบบทำงานผิดแบบเงียบ ๆ
- ปุ่มบนหน้าเว็บควรบอกเสมอว่ากำลังจะตรวจด้วย "ข้อมูลตัวอย่าง" หรือ "AI" — ตอนนี้บอกแล้ว อย่าเอาออก

---

## สรุปสิ่งที่ 5 บทนี้มีร่วมกัน

ทุกบทข้างบนคือ **ความผิดพลาดที่ระบบไม่ร้อง** ระบบยังทำงานต่อ ไม่มีข้อความ error สีแดง แต่ผลลัพธ์ผิด นี่คือประเภทของบั๊กที่โปรเจกต์นี้ต้องระวังที่สุด และเป็นเหตุผลที่กติกาใน `AGENTS.md` ยืนยันว่าคำว่า "เสร็จแล้ว" ต้องมาคู่กับผล test ดิบเสมอ
