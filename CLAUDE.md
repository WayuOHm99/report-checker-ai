# RubricLensAi

กติกาการทำงานทั้งหมดของโปรเจกต์นี้อยู่ในไฟล์ `AGENTS.md` ที่โฟลเดอร์เดียวกัน
บรรทัดข้างล่างนี้ดึงเนื้อไฟล์นั้นเข้ามาอัตโนมัติ — **อ่านและทำตามก่อนเริ่มงานทุกครั้ง**

@AGENTS.md

> ทำไมต้องมีไฟล์นี้: เครื่องมือ AI แต่ละตัวอ่านไฟล์กติกาคนละชื่อกัน
> `AGENTS.md` คือแหล่งความจริงเดียว ส่วนไฟล์นี้เป็นแค่ทางลัดให้ Claude Code เห็น
> **แก้กติกาที่ `AGENTS.md` เท่านั้น อย่าแก้ที่ไฟล์นี้**
